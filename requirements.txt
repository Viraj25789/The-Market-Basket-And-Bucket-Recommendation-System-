# Flask for web app
# ── Web framework ─────────────────────────────────────────────────────────────
Flask>=2.3,<4.0

# ── Data & ML ─────────────────────────────────────────────────────────────────
pandas>=1.5,<3.0
numpy>=1.23,<2.0
scikit-learn>=1.2,<2.0
mlxtend>=0.22,<1.0

# ── Deep learning (optional – only needed for SequencePredictor / LSTM) ───────
# Remove or comment out if you don't need LSTM predictions.
tensorflow>=2.12,<3.0

# pip install -r requirements.txt